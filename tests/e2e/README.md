# E2E tests

Playwright tests for VF frontend. Requires:
- frontend on http://localhost:5174
- `cd frontend/client && npx playwright test` (from package with @playwright/test)

Specs were moved from repo root during tool-folder cleanup (2026-07-10).
