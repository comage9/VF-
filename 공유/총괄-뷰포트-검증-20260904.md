# 총괄 뷰 좌표 오버레이 검증 — 5db2a07 후속 실측 (2026-09-04)

> **작성**: Hermes (Playwright 화면 실측) · **대상 커밋**: 5db2a07(총괄 미니맵 오버레이), ae1156b(보고서)
> **성격**: OpenClaw "총괄=A동 완전 통일(120=120)" 주장의 교차 검증 결과. 수정 지시용.

---

## 1. 결론 (요약)

OpenClaw 주장은 **번호 집합 기준으로만 맞다**. 화면 표시 기준으로는 **아직 안 맞는다.**

| 검증 항목 | 결과 |
|---|---|
| 커밋 존재 | ✅ 5db2a07·ae1156b 실제 존재 (파일 변경 확인) |
| 총괄 오버레이 코드 추가 | ✅ A_COORD_NOS 좌표 오버레이 + MiniZoneCell 칩 제거 (diff 확인) |
| 번호 집합 일치 | ✅ 총괄 120개 = A동 120개, 차이 0건 (DOM 실측) |
| **화면 안에 실제로 보이는가** | ❌ **총괄에서 120개 중 38개가 화면 밖(스크롤 필요)** |

→ "완전 통일"이 아니라 **"DOM에는 120개가 있으나 화면에는 일부만 보인다"** 가 정확한 상태.

## 2. 실측 근거 (Playwright, 뷰포트 1440×900)

- 총괄 뷰 오버레이 라벨 120개 렌더 확인 (전부 칸 영역 안 위치)
- **그중 38개가 뷰포트 밖**: `37 @ y=1070`, `29~36 @ y=918~1070`, `38 @ y=1070` 등 (900px 초과)
- 총괄 컨테이너: `flex-1 overflow-auto` — **scrollHeight 3669px** / clientHeight 819px (≈4.5배, 세로 스크롤 필요)
- A동 미니맵 좌표: x 338~836 구간, 세로가 화면 밖으로 길게 늘어남

## 3. 원인 (코드)

```
product-display.tsx:2129-2130  DONG_DEFAULT_ZOOM = { A:1.2, B:2.2, C:1.3, D:2.2 }
product-display.tsx:2131-2134  dong 전환 시 zoomFactor = DONG_DEFAULT_ZOOM[dong]
product-display.tsx:2140       setFitScale(1);   // 자동 맞춤 폐지 — 배율은 zoomFactor만
```

- 총괄(전체) 뷰에서도 **A동 기본 배율 1.2(120%)가 적용**되어 21행 그리드가 세로 1000px+로 커짐
- 총괄은 "한눈에 보는 전체 지도"인데 화면에 안 맞는 배율 → 하단 좌표(37·38·하단부)가 스크롤 밖
- 5db2a07의 검증(5595f14 스크립트)은 **DOM 존재 여부만** 확인, **뷰포트 가시성은 미검증**

## 4. 필요한 수정 (OpenClaw 지시용)

총괄(dong === 전체/총괄) 뷰에서 **그리드를 화면(컨테이너)에 맞게 자동 축소**해야 한다:

1. `dong === "총괄"(전체)`일 때는 `DONG_DEFAULT_ZOOM`을 적용하지 말 것
2. 대신 총괄 컨테이너 폭/높이에 맞춰 **auto-fit scale 계산** (모든 동 A~E가 한 화면에 들어오도록)
   - 예: `totalW / containerW`, `totalH / containerH` 중 작은 값 채택
3. 총괄에서 배율이 1보다 작아도 OK (zoomFactor 0.3~1 허용 범위) — 오버레이 좌표는 컨테이너 scale 안이라 자동 정합
4. 수정 후 실측 검증 기준:
   - 총괄 뷰에서 **120개 오버레이 전부가 스크롤 없이 뷰포트 안**에 보일 것
   - A동 단독과 번호 집합 동일 유지 (현재 ✅ 유지 확인)
   - 스크린샷: 총괄 전체가 한 화면에 (좌우/상하 스크롤 없이)

## 5. 참고

- 검증 산출물: `공유/화면실측-20260904/5db2a07-총괄.png`, `5db2a07-ADong.png`, `5db2a07-비교.json`, `5db2a07-정밀픽셀.json`
- 이전 판정: 97c18b8 "표시값 소스 교체" → 화면 무변화 (61/75존 동일) — §9 참조
- 이번은 "표시는 됐지만 뷰포트 밖" — **렌더 존재 ≠ 사용자 가시** 구분이 반복 실패의 공통 패턴
