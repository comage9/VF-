# 배치도 로케이션 개편 — 진행 상황 종합 보고 (2026-09-04)

> **작성**: OpenClaw — 사용자 요청("진행 상황 자세하게 정리해서 문서로 다시 보고")에 따른 종합 보고
> **범위**: 2026-09-04 전일(좌표 귀속 모델 ~ 카논 교정 완료) — 커밋 7건 + 실측 증거 3회

---

## 1. 최종 완성 상태 (TL;DR)

**A동 로케이션 번호 체계가 "카논(사용자 확정 규칙)"에 100% 정합**됐고, 두 뷰(총괄·A동 단독)에서 **동일하게 표시**됨을 화면 실측으로 증명했습니다.

| 항목 | 상태 |
|---|---|
| 전체 번호 연속성 | **1~151 누락 0 · 중복 0** (자동 검증 ALL PASS) |
| ①구간 (X9/X7, 1~38) | 고정 유지 — 37번(L1-1) 포함, 빈 칸도 표기 |
| ②구간 (X6/X4, 39~76) | 결함 보정 완료 — (4,14)=61, (4,18)=[69,70], 마감 76 |
| ③구간 (X3/X1, 77~135) | **카논 반전 교정 완료** — 77=(3,3)=L5-1 아래→위 뱀, L7(136~151)과 연속 |
| 좌표 귀속 | 번호는 좌표에 고정 — 존 이동·유무와 무관, 존ID 노출 0 |
| 렌더 경로 | **좌표 기준 오버레이 1벌로 통일** — 단독·총괄·모바일·엑셀 동일 소스 |
| 동별 기본 배율 | A 120% · B 220% · C 130% · D 220% 적용 |

---

## 2. 오늘(9/4) 수행한 커밋 7건 (시간순)

| 커밋 | 내용 | 핵심 |
|---|---|---|
| `97c18b8` | 좌표 귀속 모델 + A_COORD_NOS 기준표 | 번호=좌표 귀속 확정, L1-1=37 추가, 표시 전면 교체 |
| `d28b777` | L7 개념 폐기 | 하드코딩 115~122 삭제, 툴팁 "7번 라인" 제거 |
| `13da1e3` | 렌더 단일화 1차 | gridLabels 라벨 블록 제거, ZoneCell 칩 단일화 |
| `7a293da` | 동별 기본 배율 | A120·B220·C130·D220 — 동 전환 자동 적용 |
| `5601141` | **렌더·데이터 통일 (핵심 구조 변경)** | X열 9열 고정, 좌표 기준 오버레이(존 무관 직접 렌더), 측면 칩 폐기 |
| `1ad79e7` | **③구간 카논 반전 교정** | 77=(3,3)=L5-1 아래→위 뱀, L6-19=135 종료 |
| `5194f39` | **②구간 결함 보정** | 69/70 누락·72~75 중복 해소 — 1~151 완전 연속 달성 |
| `6e6cddb` | 카논 검증 스크립트 | 소스 맵 직접 파싱 자동 검증 (재현성 확보) |

---

## 3. 구조적 전환 — "왜 이번엔 먹혔나"

### 3-1. 이전 실패 원인 (Hermes 진단 확정)
- 렌더 경로 5벌 분화 — 한 뷰 고치면 다른 뷰 깨짐
- 데이터 소스 3중(존 저장값·정적 맵·좌표표) — 기준 갈림
- **검증 루프 부재** — tsc·빌드만 통과하고 실제 화면 미확인 채 "완료" 반복

### 3-2. 이번 해법 (3대 전략 적용)
1. **렌더 1벌 통일**: `gridLabels`의 좌표 기준 오버레이 — `A_COORD_NOS` 전체 키를 **그리드 픽셀(9열 고정 52px·행 38px)에 직접 렌더**. 존 위치·유무와 무관하므로 "번호가 칸을 따라가는" 문제가 구조적으로 소멸
2. **카논 SSOT**: 맵(A_LOCNO_MAP/A_LOCNOS_MAP)만 카논대로 재작성 — 좌표계·렌더 로직은 건드리지 않고 값만 교정
3. **화면 증거 필수**: Playwright 실측(Hermes 구축) + JSON 텍스트 덤프 판정 — 이번 보고는 전부 실측 기반

---

## 4. 화면 실측 증거 (최종 판정)

**측정 도구**: `frontend/client/e2e/pd-locno-evidence.spec.ts` (Playwright) — 총괄/A동 전환 캡처 + 화면 텍스트 덤프
**산출**: `공유/화면실측-20260904/` (PNG 2장 + JSON 2건, 15:03·15:35 캡처)

### 4-1. 카논 번호 — 두 뷰 모두 표시 확인 (18/18 ✅)

| 좌표 | 번호 | 총괄 | A동 |
|---|---|---|---|
| (9,3) 빈칸 | 37 | ✅ | ✅ |
| (9,12) | 20 | ✅ | ✅ |
| (9,21) | 1 | ✅ | ✅ |
| (4,14) | 61 | ✅ | ✅ |
| (4,16) | 65 | ✅ | ✅ |
| (4,18) 다품목 | 69→70 | ✅ | ✅ |
| (6,18) 다품목 | 71→72 | ✅ | ✅ |
| (4,19) | 73 | ✅ | ✅ |
| (6,19) | 74 | ✅ | ✅ |
| (4,20) | 75 | ✅ | ✅ |
| (6,20) | 76 | ✅ | ✅ |
| (3,3) **카논 시작** | 77→78 | ✅ | ✅ |
| (1,3) | 79→80 | ✅ | ✅ |
| (1,4) | 81→82 | ✅ | ✅ |
| (3,4) | 83→84 | ✅ | ✅ |
| (3,5) | 85 | ✅ | ✅ |
| (3,21) 종료 | 134 | ✅ | ✅ |
| (1,21) 종료 | 135 | ✅ | ✅ |
| L7-1 연속 | 136→137 | ✅ | ✅ |

> 이전 실측에서 "좌표 0건·번호 미표시"로 나왔던 것은 **JSON 덤프 60개 캡(limit) 오판**이었음 — 캡을 풀고 전수 분석한 결과 실제로는 정상 표시 확인

### 4-2. 자동 검증 (scripts-check-sec2.mjs — 소스 맵 직접 파싱)

```
15개 좌표 검증 전부 OK — (9,3)=37 · (4,14)=61 · (3,3)=[77,78] · (1,21)=[135] 등
전체: 151개, 1~151 누락: [], 중복: []
→ ALL PASS
```

---

## 5. 사용자 보고 증상 → 해소 매핑

| 보고 증상 | 원인 | 해소 방법 | 현재 |
|---|---|---|---|
| ① (9,12)의 20이 우측으로 밀려남 | A-NEW 칸 line=-1 → 홀짝 판정 오류 | 측면 칩 폐기 → 좌표 기준 오버레이 | ✅ 표시됨 (2뷰) |
| ② (4,14)의 61이 (4,16)에 표기 | 존 배치와 번호 어긋남(존 귀속) | 좌표 귀속 + X열 9열 고정 | ✅ (4,14)=61, (4,16)=65 |
| ③ (4,14)/(6,14) 번호 누락 | 저장값 소실(스테일) | 좌표 귀속 오버레이 — 존 저장값 무관 | ✅ 61·62 표시 |
| ③구간(77~) 상하 반전 | 맵이 위→아래 | 카논 재부여(77=(3,3) 아래→위) | ✅ 반영 |
| ②구간 69/70 누락·72~75 중복 | 다품목 이중 정의 | 슬롯16 다품목 소진 방식으로 재배정 | ✅ 연속 1~151 |
| 총괄/A동 표기 불일치 | 렌더 5벌 분화 | 오버레이 1벌 통일 | ✅ 동일 표시 |
| 빈 칸 37 미표시 | 맵 결번 | L1-1=37 추가 + 존 무관 오버레이 | ✅ 표시됨 |

---

## 6. 남은 항목 (다음 단계)

| 항목 | 상태 | 비고 |
|---|---|---|
| **사용자 최종 화면 검수** | ⏳ 대기 | 브라우저 강제 새로고침(Ctrl+Shift+R) 후 확인 — 캐시 이슈 주의 |
| 다품목 구획 세로 순서 정합 (위=item0 vs 낮은번호=아래) | 미착수 | 현장 관례 확인 후 결정 |
| L7 하단(136~151)·X1/X2·신규 칸 번호 최종 확정 | 현장 확인 필요 | 현재 맵값 유지 |
| 총괄 뷰에 "X,Y" 텍스트 상시 표기 추가 | 옵션 | 현재는 호버 툴팁 + 축 라벨 방식 |

---

## 7. 커밋 이력 (오늘 전체)

```
6e6cddb chore(pd): ②③구간 카논 검증 스크립트 추가
5194f39 fix(pd): ②구간(L3/L4) 결함 보정 — 69/70 누락·72~75 중복 해소
1ad79e7 feat(pd): ③구간(L5/L6) 카논 반전 교정 — 77=(3,3) 아래→위 뱀
5601141 refactor(pd): 렌더·데이터 통일 — 좌표 기준 오버레이 1벌
7a293da feat(pd): 동별 기본 배율 적용 (A120·B220·C130·D220)
13da1e3 refactor(pd): 로케이션 번호 렌더 단일화
d28b777 refactor(pd): L7 개념 폐기
97c18b8 feat(pd): 로케이션 번호 좌표 귀속 모델 — A_COORD_NOS
```

---

## 8. 검증 재현 방법

```bash
# 1) 코드 정적 검증 (소스 맵 → 좌표별 번호 + 연속성)
node frontend/client/scripts-check-sec2.mjs
# 기대: 15개 좌표 OK + "전체: 151개, 누락 [], 중복 []" → ALL PASS

# 2) 화면 실측 (개발서버 5174 기동 상태에서)
cd frontend/client && npx playwright test --config=playwright.evidence.config.ts e2e/pd-locno-evidence.spec.ts
# 산출: 공유/화면실측-20260904/ (PNG+JSON)
```


---

## 9. 최종 실측 보고 — 직접 브라우저 강제 새로고침 검증 (2026-09-04 17:45)

**방법**: Playwright로 localhost:5174 접속 → 강제 새로고침(reload) → 총괄/A동 전환 → **DOM에서 렌더된 amber 라벨 전수 추출**(텍스트+화면중심좌표) → 중복·배율 판정. 스크립트: `frontend/client/e2e/dom-verify.spec.ts`, 산출: `공유/화면실측-20260904/dom-검증.json` + `직접검증-*.png` 5장.

| 판정 항목 | A동 단독 | 총괄 |
|---|---|---|
| 카논 19개 대표 번호 표시 | ✅ 19/19 | ✅ 19/19 |
| 중복 렌더(칸당 1개 원칙) | **0개 ✅** | **0개 ✅** |
| 동별 기본 배율 | A=120% | — |

- 전 동 배율 실측: **A 120% · B 220% · C 130% · D 220%** — 지정값 정확 적용 확인
- 마지막 발견건(다품목 구획 단일 번호 반복 → 75×3·127×4 등 중복) 수정 완료: 커밋 `dc4cdd0` — locNos.length>1인 경우에만 구획별 개별 번호 표시
- **판정: 사용자 보고 증상 전부 해소, 카논 정합, 두 뷰 일치 → 완료**


---

## 10. 63번 위치 추적·교정 (2026-09-04 18:10)

**사용자 문의**: "로케이션 번호 63번 어디 갔는지"

**추적 결과**: 63은 사라진 게 아니라 **(4,15)에 잘못 표시**되고 있었음. 올바른 위치는 **(6,15)**.

**근거 3중 일치**:
1. 사용자 확정 앵커로 사다리 뱀 역산: 61=(4,14) → 62=(6,14) → **63=(6,15)** → 64=(4,15) → 65=(4,16)
2. 저장 스냅샷 v326 기록: `A-L3-13 locNo=63` · `A-L4-13 locNo=64` — (6,15)=63이 원래 데이터
3. 홀수 행 교대 규칙: Y15·17·19는 X6 우선 (①②③구간 전체 일관)

**원인**: 이전 ②구간 보정(5194f39)에서 슬롯 13·15·17의 좌우 쌍을 반대로 배정 — 저장 스냅샷은 정확했음.

**교정** (커밋 `dab0a12`): 슬롯 13·15·17 쌍 스왑 — L4-13=64·L3-13=63 / L4-15=68·L3-15=67 / L4-17=74·L3-17=73

**재실측 판정**: 검증 스크립트 19좌표 전부 OK (6-15=[63]·4-15=[64] 등) + 1~151 누락 0·중복 0. Playwright DOM 재실측 — **63이 두 뷰 각 1회 표시**(A동 화면중심 x=644,y=723 = X6열·Y15행 자리), 중복 렌더 0건.
