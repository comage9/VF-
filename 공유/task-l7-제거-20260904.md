# 정밀 편집 지시서: L7 개념 폐기 — 좌표 통일 마무리

> 아래 변경을 **정확히 그대로** 적용하라. 분석·재설계 금지.

## Edit 1 — L7 빌더 하드코딩 번호 제거 (frontend/client/src/pages/product-display.tsx)

앵커(정확 일치):
```
          showNumAsProduct: false,
          productNamesVertical: true, // 가로 배치 칸: 제품명 세로 나열 (2026-08-29)
          locNo: 115 + i, // 로케이션 번호 115~122
          style: {
```
교체(가운데 줄 삭제):
```
          showNumAsProduct: false,
          productNamesVertical: true, // 가로 배치 칸: 제품명 세로 나열 (2026-08-29)
          style: {
```

## Edit 2 — 레이아웃 로드 시 L7 잔재 번호 정리

앵커:
```
        const parsed = JSON.parse(raw);
        if (parsed && parsed.__v === LAYOUT_VERSION && Array.isArray(parsed.layout) && parsed.layout.length === DONG_LAYOUTS.length) {
          return hydrateZoneSplitDir(parsed.layout as typeof DONG_LAYOUTS);
        }
```
교체:
```
        const parsed = JSON.parse(raw);
        if (parsed && parsed.__v === LAYOUT_VERSION && Array.isArray(parsed.layout) && parsed.layout.length === DONG_LAYOUTS.length) {
          const restored = hydrateZoneSplitDir(parsed.layout as typeof DONG_LAYOUTS);
          // L7 잔재 번호 정리 (2026-09-04): 구 빌더 하드코딩(115~122) 제거 — 모든 칸은 좌표로만 식별
          return restored.map((d) => ({
            ...d,
            zones: d.zones.map((z) =>
              /^A-L7-\d+$/.test(z.id) && typeof z.locNo === "number" && z.locNo >= 115 && z.locNo <= 122
                ? { ...z, locNo: undefined }
                : z
            ),
          }));
        }
```

## Edit 3 — 툴팁 "7번 라인" 표기 2곳 삭제

앵커 1 (들여쓰기 6칸):
```
      if (isL7) parts.push("7번 라인 (슬림서랍장·칵투스)");
```
→ 해당 줄 삭제.

앵커 2 (들여쓰기 4칸):
```
    if (isL7) parts.push("7번 라인 (슬림서랍장·칵투스)");
```
→ 해당 줄 삭제.

삭제 후 `const isL7 = z.line === 7;` 선언이 사용처 없이 남으면(각 함수별로) 그 선언줄도 함께 제거한다. tsc에서 TS6133으로 확인 가능.

## 금지 사항

- 좌표 코드(gridLabels, buildGridCoordSystem 등)·locNos 미러링 코드 **수정 금지**
- A_LOCNO_MAP/A_LOCNOS_MAP의 L7 데이터 항목은 **삭제 금지** (데이터 보존)
- L7 마이그레이션·정렬·splitDir 등 내부 동작 로직 **수정 금지**
- 그 외 모든 변경 금지

## 검증 (마지막에 반드시 실행)

1. `frontend/client`에서 `npx tsc --noEmit 2>&1 | grep product-display | grep -v TS6133` → 신규 오류 0건
2. `npm run build` 통과
3. `grep -c "115 + i" frontend/client/src/pages/product-display.tsx` → 0
4. `grep -c "7번 라인" frontend/client/src/pages/product-display.tsx` → 0
5. `grep -c "locNo: 115" frontend/client/src/pages/product-display.tsx` → 0
6. 변경 요약 출력
