# 작업계획 — 배치도 렌더·번호 단일화 (2026-09-04)

> 상태: **사용자 승인 대기 → 승인 후 OpenClaw(+opencode) 실행, Hermes는 실측 검증 담당**
> 규약: `에이전트-협업-규약.md` (대형 변경 전 계획서+승인) · 기준: `로케이션-번호-밀림-검토-20260904.md` §7.5·§9

## 1. 배경 — 반복 실패의 구조적 원인 (왜 패치가 계속 안 먹혔나)

1. **렌더 경로 5벌 분화**: 단독뷰(gridLabels)·전체뷰(MiniZoneCell 칩)·로케이션 리스트·인쇄·엑셀 — 저마다 게이트/폴백/위치 규칙 → 한 뷰 고치면 다른 뷰 깨짐 (증상 1·2)
2. **번호 데이터 소스 3중**: 존 저장값(zone.locNo)·정적 맵·좌표표(A_COORD_NOS) + localStorage 스테일 → "어느 게 기준?" 갈림
3. **번호 렌더가 존(칸) 위치 앵커**: 좌표 기준으로 값을 바꿔도 그리는 위치가 존 픽셀 → (4,14)=61이 (4,16)에 표기되는 문제 재현 (97c18b8 검증됨)
4. **검증 루프 부재**: tsc·빌드·정적 시뮬만 통과, **실제 브라우저 화면 실측 없음** → 외관 변화 0인 채 "완료" 커밋 반복

## 2. 사용자 확인 증상 (2026-09-04 실측 재현 완료)

| # | 증상 | 실측 증거 (공유/화면실측-20260904/) |
|---|---|---|
| 1 | 총괄 화면: 좌표(X,Y) 표기 없음 | 총괄-전체뷰.png/json — 번호만(134→135 등), X 좌표 0 |
| 2 | A동 단독: 로케이션 일부만 + X,Y 좌표 표기 없음 | ADong-단독뷰.png/json — 축 숫자(1,2,3…)만 |
| 3 | 로케이션 20(제품 620) 우측 밀림 | A-L1-10 존 부재 → 20이 A-NEW-91001 등 엉뚱한 칸에 표기 (총괄 PNG) |
| 부가 | ③구간(77~) 번호 매핑 상하 반전 | 카논: 77=(3,3) 아래→위 vs 현재 맵 77=(3,21) 위→아래 |

## 3. 실행 계획

**P0 — 화면 실측 검증 환경 (Hermes — ✅ 완료 2026-09-04)**
- `frontend/client/playwright.evidence.config.ts` + `e2e/pd-locno-evidence.spec.ts`
- 실행: `cd frontend/client && npx playwright test --config=playwright.evidence.config.ts e2e/pd-locno-evidence.spec.ts`
- 산출: `공유/화면실측-20260904/` (뷰별 PNG + 텍스트/번호 덤프 JSON)
- ※ 우회: playwright는 ubuntu26.04 미지원 → 캐시 Chromium `chrome-linux64/chrome` executablePath 지정

**P1 — 렌더 단일화 (OpenClaw 실행, 1커밋 목표)**
- (a) 번호 라벨을 **좌표 기준 오버레이 1벌**로: A_COORD_NOS 전체 키를 그리드 픽셀(열 52px·행 38px, L7=Y1)에 직접 렌더, 존 위치·유무 무관 → 단독·총괄이 **같은 함수** 사용 (2047-2080행 교체)
- (b) MiniZoneCell z.locNo 폴백 제거 (5682-5690) — 옛 번호(스테일 80 등) 차단
- (c) 총괄 칩 좌/우 -18px → 단독과 동일 배치 (5687)
- (d) A동 X열 **기준 9열 고정**(52px 피치) — 점유 열 클러스터 계산 폐기 (400-438행) → 재빌드 시 11열 붕괴·L1열 전멸 방지

**P2 — 데이터 SSOT·스코프 (같은 커밋 또는 직후)**
- (e) **A_COORD_NOS를 사용자 카논으로 재작성**: ①(X9/X7 위→아래) ②(X6/X4 아래→위) ③(X3/X1 **아래→위, 77=(3,3)**) — 현재 ③ 상하 반전 교정 + 다품목(77~151)·L7 포함 연속성 검증
- (f) A_COORD_NOS 적용을 **A동 스코프** ('A-' 접두) — B/C/D 오염(B28·C46·D27존) 차단 (1853-1864·3688-3699행)
- (g) layout 복원 시 A동 zone locNo/locNos **제거**(표시는 좌표표 단일) (1273-1295행) → 로케이션 리스트·인쇄·엑셀도 좌표표 사용으로 정리(3811·4017·4039)
- (h) 다품목 칸: skip(2053-2055) 폐기 → 구획 표시(6385-6411)와 중복 없는 요약

**P3 — 검증 (변경자 + Hermes 교차)**
- tsc 0건·빌드 통과 후 **P0 실측 스크립트 재실행** → 총괄/단독 PNG 비교
- 통과 기준: ① 총괄에도 좌표(X,Y) 표기 ② A동 단독 1~151 전체 번호 표시(빈 칸 포함) ③ (4,14)=61이 (4,16)이 아닌 (4,14)에 표기 ④ 번호-좌표 카논 3구간 일치 ⑤ B/C/D에 A동 번호 없음 ⑥ 제품 위치 불변
- 스크린샷을 사용자에게 제출 → 최종 승인

## 4. 파일 대상
- `frontend/client/src/pages/product-display.tsx` (렌더/데이터 — 주요)
- (필요 시) `frontend/client/src/pages/product-display-utils.ts`

## 5. 검증 기준·리스크
- 제품 배치(placement) 데이터는 **건드리지 않는다** (표시만 수정)
- localStorage 스테일이 남아 있으면 1회 "초기화 후 재실측" 필요할 수 있음
- 완료 후 커밋 메시지에 "실측 검증 스크린샷: 공유/화면실측-20260904/" 명시
